<div class="container first-container">
  <div class="row">

    <div class="col-md-8 col-md-offset-2">

      <form id="form-register-details" class="form-horizontal card">
        <div class="alert alert-success"><p><strong>注册成功！</strong>请完善你的信息，以便获得基于你的兴趣的推荐。</p></div>
        <div class="panel panel-info">
          <div class="panel-heading">
            <h3 class="panel-title">完善信息</h3>
          </div>
          <div class="panel-body">
            {{#parseJSON '{"id": "actual_name", "label": "真实姓名", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}
            <div class="form-group">
              <label class="col-xs-2 control-label">
                性别
              </label>
              <div class="col-xs-10">
                <div class="radio-inline">
                  <label>
                    <input type="radio" name="sex" id="stu_gender_m" value="Male" checked>
                    男
                  </label>
                </div>
                <div class="radio-inline">
                  <label>
                    <input type="radio" name="sex" id="stu_gender_f" value="Female">
                    女
                  </label>
                </div>
              </div>
            </div>
            {{#parseJSON '{"id": "birthday", "label": "生日", "horizontal": true, "type": "date"}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "telephone", "label": "手机号码", "horizontal": true, "type": "tel"}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "qq", "label": "QQ号码", "horizontal": true, "type": "tel"}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "department", "label": "院系", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "undergraduate_course", "label": "本科专业", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "class", "label": "班级", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "year", "label": "入学年份", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "snumber", "label": "学号", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}

            {{#parseJSON '{"id": "company", "label": "公司", "horizontal": true}'}}
              {{> form-group }}
            {{/parseJSON}}
            
          </div>

        </div>

        <div class="panel panel-danger">
          <div class="panel-heading">
            <h3 class="panel-title">推荐关注</h3>
          </div>
          <div class="panel-body">
            
          </div>
        </div>

        <div class="form-group">
          <div class="col-md-10 col-md-offset-2">
            <a href="home.html" class="btn btn-primary btn-lg">确定</a> <a href="#form-login" data-toggle="tab" class="btn btn-lg btn-default">跳过</a>
          </div> 
        </div>
      </form>
    </div>
  </div>
</div>